backtrace.patch

From: Christian Prochaska <christian.prochaska@genode-labs.com>


---
 gcc/diagnostic-show-locus.c |    2 ++
 gcc/diagnostic.c            |    7 ++++++-
 2 files changed, 8 insertions(+), 1 deletion(-)

diff --git a/gcc/diagnostic-show-locus.c b/gcc/diagnostic-show-locus.c
index bf95666..b68b700 100644
--- a/gcc/diagnostic-show-locus.c
+++ b/gcc/diagnostic-show-locus.c
@@ -24,7 +24,9 @@ along with GCC; see the file COPYING3.  If not see
 #include "version.h"
 #include "demangle.h"
 #include "intl.h"
+#if 0
 #include "backtrace.h"
+#endif
 #include "diagnostic.h"
 #include "diagnostic-color.h"
 
diff --git a/gcc/diagnostic.c b/gcc/diagnostic.c
index 8106172..efcc053 100644
--- a/gcc/diagnostic.c
+++ b/gcc/diagnostic.c
@@ -28,7 +28,9 @@ along with GCC; see the file COPYING3.  If not see
 #include "version.h"
 #include "demangle.h"
 #include "intl.h"
+#if 0
 #include "backtrace.h"
+#endif
 #include "diagnostic.h"
 #include "diagnostic-color.h"
 
@@ -468,13 +470,16 @@ diagnostic_action_after_output (diagnostic_context *context,
     case DK_ICE_NOBT:
       {
 	struct backtrace_state *state = NULL;
+#if 0
 	if (diag_kind == DK_ICE)
 	  state = backtrace_create_state (NULL, 0, bt_err_callback, NULL);
+#endif
 	int count = 0;
+#if 0
 	if (state != NULL)
 	  backtrace_full (state, 2, bt_callback, bt_err_callback,
 			  (void *) &count);
-
+#endif
 	if (context->abort_on_error)
 	  real_abort ();
 

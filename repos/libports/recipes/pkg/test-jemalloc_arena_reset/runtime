<runtime ram="300M" caps="1000" binary="init">

	<requires> <timer/> </requires>

	<events>
		<timeout meaning="failed" sec="5" />
		<log meaning="succeeded">child "test-jemalloc_arena_reset" exited with exit value 0</log>
	</events>

	<content>
		<rom label="ld.lib.so"/>
		<rom label="libc.lib.so"/>
		<rom label="libm.lib.so"/>
		<rom label="posix.lib.so"/>
		<rom label="vfs.lib.so"/>
		<rom label="jemalloc.lib.so"/>
		<rom label="jemalloc_test.lib.so"/>
		<rom label="test-jemalloc_arena_reset"/>
	</content>

	<config>
		<parent-provides>
			<service name="ROM"/>
			<service name="IRQ"/>
			<service name="IO_MEM"/>
			<service name="IO_PORT"/>
			<service name="PD"/>
			<service name="RM"/>
			<service name="CPU"/>
			<service name="LOG"/>
			<service name="Timer"/>
		</parent-provides>
		<default-route>
			<any-service> <parent/> <any-child/> </any-service>
		</default-route>
		<default caps="200"/>
		<start name="test-jemalloc_arena_reset">
			<resource name="RAM" quantum="290M"/>
			<config>
				<vfs> <dir name="dev"> <log/> </dir> </vfs>
				<libc stdout="/dev/log" stderr="/dev/log"/>
			</config>
		</start>
	</config>
</runtime>

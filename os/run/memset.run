build "core init test/memset"

create_boot_directory

install_config {
	<config>
		<parent-provides>
			<service name="ROM"/>
			<service name="RAM"/>
			<service name="CPU"/>
			<service name="RM"/>
			<service name="CAP"/>
			<service name="PD"/>
			<service name="IRQ"/>
			<service name="IO_PORT"/>
			<service name="SIGNAL"/>
			<service name="LOG"/>
		</parent-provides>
		<default-route>
			<any-service> <parent/> <any-child/> </any-service>
		</default-route>
		<start name="test-memset">
			<resource name="RAM" quantum="2G"/>
		</start>
	</config>
}

build_boot_image "core init test-memset"

append qemu_args "-nographic -m 1024M"

run_genode_until {child exited with exit value 0.*} 200

puts "Test succeeded"

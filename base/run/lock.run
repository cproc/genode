build { core init test/lock }

create_boot_directory

install_config {
	<config>
		<parent-provides>
			<service name="RM"/>
			<service name="ROM"/>
			<service name="LOG"/>
		</parent-provides>
		<default-route>
			<any-service> <parent/> <any-child/> </any-service>
		</default-route>
		<start name="test-lock">
			<resource name="RAM" quantum="1M"/>
		</start>
	</config>
}

set boot_modules { core init test-lock }

build_boot_image $boot_modules

append qemu_args "-nographic -m 64"

run_genode_until forever


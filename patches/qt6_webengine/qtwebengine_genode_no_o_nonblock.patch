Fix unhandled EWOULDBLOCK error when downloading files with Falkon.

From: Christian Prochaska <christian.prochaska@genode-labs.com>


---
 .../chromium/base/files/file_util_posix.cc         |   10 ++++++++++
 1 file changed, 10 insertions(+)

diff --git a/src/3rdparty/chromium/base/files/file_util_posix.cc b/src/3rdparty/chromium/base/files/file_util_posix.cc
index 034c92e6f..00776f829 100644
--- a/src/3rdparty/chromium/base/files/file_util_posix.cc
+++ b/src/3rdparty/chromium/base/files/file_util_posix.cc
@@ -215,8 +215,13 @@ bool DoCopyDirectory(const FilePath& from_path,
       continue;
     }
 
+#if BUILDFLAG(IS_GENODE)
+    /* O_NONBLOCK could cause unhandled EWOULDBLOCK with regular files */
+    File infile(open(current.value().c_str(), O_RDONLY));
+#else
     // Add O_NONBLOCK so we can't block opening a pipe.
     File infile(open(current.value().c_str(), O_RDONLY | O_NONBLOCK));
+#endif
     if (!infile.IsValid()) {
       DPLOG(ERROR) << "CopyDirectory() couldn't open file: " << current.value();
       return false;
@@ -243,7 +248,12 @@ bool DoCopyDirectory(const FilePath& from_path,
     if (open_exclusive)
       open_flags |= O_EXCL;
     else
+#if BUILDFLAG(IS_GENODE)
+      /* O_NONBLOCK could cause unhandled EWOULDBLOCK with regular files */
+      open_flags |= O_TRUNC;
+#else
       open_flags |= O_TRUNC | O_NONBLOCK;
+#endif
     // Each platform has different default file opening modes for CopyFile which
     // we want to replicate here. On OS X, we use copyfile(3) which takes the
     // source file's permissions into account. On the other platforms, we just
